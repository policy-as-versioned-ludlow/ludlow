kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
name: ludlow
# One control-plane node is enough for the talk. Keep it boring — the demo's
# interesting parts are Flux + Kyverno, not the topology.
nodes:
  - role: control-plane
